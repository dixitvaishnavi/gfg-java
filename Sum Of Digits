class Solution{
    static int sumOfDigits(int N) {
        // code here
          int temp=N,sum=0;
       while(temp>0)
       {
           sum+=temp%10;
           temp=temp/10;
       }
       return sum;

    }
}
