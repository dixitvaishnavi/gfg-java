class Solution {
    static String delAlternate(String S) {
        // code here
           String res ="";
       for(int i=0; i<S.length(); i=i+2)
       {
           res = res+S.charAt(i);
          
       }
        return res;

    }
}
