class GfG
{
	boolean isBinary(String str)
	{
	  //Your code here
	       int n=str.length();int count=0;boolean b=false;
       for(int i=0; i<n ; i++)
       if(str.charAt(i)=='0'||str.charAt(i)=='1')count++;
       if(n==count)b=true;
       return b;

	}
}
